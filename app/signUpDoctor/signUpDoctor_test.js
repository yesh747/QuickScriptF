'use strict';

describe('myApp.loginDoctor module', function() {

  beforeEach(module('myApp.loginDoctor'));

  describe('loginDoctor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var loginDoctor = $controller('loginDoctor');
      expect(loginDoctor).toBeDefined();
    }));

  });
});