'use strict';

describe('myApp.patientsView module', function() {

  beforeEach(module('myApp.patientsView'));

  describe('patientsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PatientsViewCtrl = $controller('PatientsViewCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});